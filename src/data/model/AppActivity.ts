//prettier-ignore
export enum AppActivity {
  MainIndex   = 'main-index',
  AACanvas    = 'aa-canvas',
  MLTSelector = 'mlt-selector',
  TextEditor  = 'text-editor',
  Config      = 'config'
}

//prettier-ignore
export enum AppModeState {
  Update = 'update',
  Add    = 'add',
  Cancel = 'cancel',
  Edit   = 'edit',
  Select = 'select',
  Split  = 'split'
}
